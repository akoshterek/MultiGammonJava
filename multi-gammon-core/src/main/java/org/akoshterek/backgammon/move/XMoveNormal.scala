package org.akoshterek.backgammon.move

class XMoveNormal {
  /* Move made. */
  var anMove: ChequersMove = new ChequersMove
  /* index into the movelist of the move that was made */
  var iMove: Int = Integer.MAX_VALUE
}