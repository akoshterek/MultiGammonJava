package org.akoshterek.backgammon.nn

class InputUnit extends Neuron {
  /**
    * Recomputes the value of this hidden unit, querying it's
    * prior inputs.
    */
  override def recompute(): Double = 0
}